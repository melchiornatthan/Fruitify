import { useEffect as t } from "react";
import e from "prop-types";
import { maintainScrollVisibility as p } from "./utils.js";
const o = ({ options: i, activeOptionRef: r }) => (t(() => {
  r.current && p(r.current);
}, []), i);
o.defaultProps = {
  activeOptionRef: void 0
};
o.propTypes = {
  options: e.array.isRequired
};
export {
  o as ComboboxList
};
