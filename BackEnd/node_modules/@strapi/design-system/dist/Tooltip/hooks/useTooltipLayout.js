import { useRef as l, useLayoutEffect as u } from "react";
import { positionTooltip as c } from "../utils/positionTooltip.js";
const g = (t, n) => {
  const e = l(null), p = l(null);
  return u(() => {
    if (t) {
      const o = e.current, s = p.current, r = c(o, s, n);
      o.style.left = `${r.left}px`, o.style.top = `${r.top}px`;
    }
  }, [t]), { tooltipWrapperRef: e, toggleSourceRef: p };
};
export {
  g as useTooltipLayout
};
